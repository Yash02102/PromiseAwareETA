# Analysis Artifacts

The analysis scripts write reproducible outputs into this directory.

## Layout
- `eda_summary.csv` default summary generated by `make eda`.
- `reports/` place script-generated markdown or HTML narratives (committed as needed).
- `figures/` store static plots exported by analysis scripts.

Use `.gitignore` entries to avoid committing large intermediate files; only persist lightweight summaries or rendered reports.
